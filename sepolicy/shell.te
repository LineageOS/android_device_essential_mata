allow shell ota_package_file:dir {search };
allow shell ota_package_file:file { read open };
allow shell sysfs:dir { r_dir_perms };
allow shell sysfs:file { r_file_perms };
allow shell usb_device:chr_file { open };
allow shell usb_device:dir { search };
set_prop(shell, fastboot_prop)
